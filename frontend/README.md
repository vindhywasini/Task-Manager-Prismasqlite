# Task Management System - Frontend (Next.js App Router)

## Setup

1. Install deps:
   npm install

2. Run dev server:
   npm run dev

Notes:
- Frontend expects backend at http://localhost:4000
- Login sets accessToken to localStorage and refresh token is set as cookie by backend.
